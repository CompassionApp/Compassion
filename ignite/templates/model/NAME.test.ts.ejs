import { <%= props.pascalCaseName %>Model } from "./<%= props.kebabCaseName %>"

describe("<%= props.pascalCaseName %>Model", () => {
  it("can be created", () => {
    const instance = <%= props.pascalCaseName %>Model.create({})

    expect(instance).toBeTruthy()
  })
})
